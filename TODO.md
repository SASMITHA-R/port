- [ ] Fix web resume downloader asset path/name to match pubspec.yaml
- [ ] Check/ensure Firebase.initializeApp is called with DefaultFirebaseOptions (if Firebase runtime error persists)
- [ ] Run `flutter analyze` and `flutter run` (web or target platform) to confirm build passes

